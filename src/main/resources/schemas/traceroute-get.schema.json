{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Traceroute GET Request",
  "description": "JSON schema for validating the headers and query parameters of a GET request to /traceroute",
  "type": "object",
  "properties": {
    "queryParams": {
      "type": "object",
      "properties": {
        "host": {
          "description": "The domain or ip of the host to traceroute",
          "type": "string"
        }
      },
      "required": [
        "host"
      ]
    },
    "headers": {
      "type": "object",
      "description": "HTTP headers for the request. No specific headers are required for this endpoint.",
      "properties": {},
      "additionalProperties": true
    }
  },
  "required": [
    "queryParams"
  ]
}


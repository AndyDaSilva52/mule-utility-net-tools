{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DNS GET Request",
  "description": "JSON schema for validating the headers and query parameters of a GET request to /dns",
  "type": "object",
  "properties": {
    "queryParameters": {
      "type": "object",
      "properties": {
        "host": {
          "description": "The the fully qualified domain name the host to test",
          "type": "string"
        },
        "dnsServer": {
          "description": "The DNS server to use for resolution.",
          "type": "string"
        }
      },
      "required": [
        "host"
      ]
    },
    "headers": {
      "type": "object",
      "description": "HTTP headers for the request. No specific headers are required for this endpoint.",
      "properties": {},
      "additionalProperties": true
    }
  },
  "required": [
    "queryParameters"
  ]
}


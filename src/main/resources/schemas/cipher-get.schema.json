{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cipher Test GET Request",
  "description": "JSON schema for validating the headers and query parameters of a GET request to /ciphertest",
  "type": "object",
  "properties": {
    "queryParameters": {
      "type": "object",
      "properties": {
        "host": {
          "description": "The domain or ip of the host to connect to",
          "type": "string"
        },
        "port": {
          "description": "The port number of host to be tested",
          "type": "number"
        }
      },
      "required": [
        "host",
        "port"
      ]
    },
    "headers": {
      "type": "object",
      "description": "HTTP headers for the request. No specific headers are required for this endpoint.",
      "properties": {},
      "additionalProperties": true
    }
  },
  "required": [
    "queryParameters"
  ]
}

